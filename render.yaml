services:
  - type: web
    name: qoretools
    env: dotnet
    plan: free
    buildCommand: dotnet publish -c Release -o out
    startCommand: dotnet out/QoreTools.dll --urls "http://0.0.0.0:${PORT}"
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
